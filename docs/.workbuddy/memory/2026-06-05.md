# 2026-06-05 工作日志

## 链接批量修正

- 新建 `_/scripts/fix_links.py`：将 zh/ 目录下 HTML 中的 `/docs/manual/en/` 导航链接改为 `/docs/manual/zh/`
  - `class="version"` 链接保留 en（切换到英文版的入口）
  - tppabs 属性同步替换
  - 处理结果：225 个文件，共修正 10043 处链接
- 将 `fix_nav_links()` 函数集成到 `translate_html.py`，后续翻译新文件自动处理链接
- 背景发现：commands/ 子目录用绝对路径（/docs/manual/en/...），顶层页面用相对路径（../commands/...），fix_nav_links 对相对路径无影响

## 目录页命令名修复

- 发现问题：目录页（如 `zh/commands/Conic_Commands/index.htm`）中 `<a class="xref page">` 链接的命令名被翻译为中文（如 Axes→坐标轴、Circle→圆、Ellipse→椭圆），应保持英文
- 新建 `_/scripts/fix_command_names.py`：批量扫描所有 zh/commands/*/index.htm 和 zh/tools/*/index.htm，将命令名还原为英文（从 href URL 路径提取）
  - 修复结果：295 个文件，1075 处命令名还原
- 在 `translate_html.py` 中新增 `fix_command_link_text()` 函数，翻译新文件时自动处理
  - 在 `fix_nav_links()` 之后调用，确保后续翻译不再出此问题
