# 2026-06-04 工作日志

## 命令语法标题保留英文

- **需求**：`Angle( <Object> )`、`Angle( <Line>, <Line> )` 这类命令小标题全部保留英文，不翻译
- **双重修复**：
  1. `SKIP_CLASSES` 新增 `"hdlist1"` —— Antora 用 `<dt class="hdlist1">` 包裹命令语法标题，整个父元素跳过
  2. `should_skip_node()` 新增正则判断：文本匹配 `\b[A-Z][A-Za-z]+\s*\(` 且含 `<` 或 `&lt;` 则跳过（兜底处理 `<em>` 内的命令语法如 `Rotate( <Point>, <Angle>, <Apex> )`）
- 验证：Angle 页所有 `hdlist1` 标题（7 处）全部保留英文，If 页 `If( <Condition>, <Then> )` 等 3 处保留英文，正文翻译正常
- 重新翻译 Angle、If、Slider 三个文件验证通过

## 新增阿里千问 API 支持

- 本地 Gemini 代理频繁限流，新增阿里千问 API 后端
- 配置：`https://dashscope.aliyuncs.com/compatible-mode/v1`，模型 `qwen3.7-max`（注意小写，大写 Qwen3.7-Max 会 404）
- 新增 `call_qwen_translate()` 函数：HTTPS 连接、OpenAI 兼容格式、自动去除 Qwen3 `<think/>` 推理块
- CLI 新增 `--api qwen` 选项
- 用千问翻译 Boolean_values、Input_Bar、Numbers_and_Angles 三个文件，质量很好
- 千问翻译质量评估：侧边栏、面包屑、正文、表格、Note 块、版本按钮、CSS content 全部翻译正确

## API Key 明文问题修复

- 用户发现 translate_html.py 中 Gemini 和 Qwen 的 API Key 明文写在代码中，存在安全隐患
- 修复：所有 API Key 改为通过环境变量读取
  - `GEMINI_API_KEY` / `GEMINI_BASE_URL` — Gemini 代理
  - `QWEN_API_KEY` / `QWEN_BASE_URL` / `QWEN_MODEL` — 阿里千问
  - `GLM_API_KEY` / `ZHIPUAI_API_KEY` — 智谱 GLM
- 新增 Qwen API Key 缺失时的启动检查（报错退出）
- 文件头部用法说明更新了所有环境变量文档
- MEMORY.md 中也清除了明文 Key，改为环境变量说明

## 批量翻译续传（第1天）

- 从 42/688 续传，使用 Qwen API（`qwen3.7-max`，环境变量 `QWEN_API_KEY`）
- 每批约10分钟，共24轮，翻译至 370/688（新增328个文件）
- Qwen API 全程稳定，无限流/超时，无报错
- 后台任务 stdout 未捕获（显示no output），但文件正常生成，不影响结果
- 断点续传正常：已存在的 zh 文件自动跳过
- 明天继续 `--batch` 即可，剩余约 318 个文件