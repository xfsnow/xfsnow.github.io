# 2026-06-11

## 批量替换 /_/ 路径为 /asset/

- 目标：所有 `.htm` 文件中的 `/_/css/site.css`、`/_/js/site.js` 等路径，将 `/_/` 改为 `/asset/`
- 命令：Python glob 遍历 + re.sub 批量替换
- 结果：修改了 **1376 个文件**，共替换 **3940 处** `/_/` → `/asset/`
- 涵盖 en 和 zh 两个目录下所有深度的页面（depth=2 如 `../../_/`，depth=4 如 `../../../../_/`）
- 注意：`data-ui-root-path="../../_"` 中末尾无斜杠的 `/_` 未被本次替换（不在需求范围内）

## 修正 data-ui-root-path 属性值

- `data-ui-root-path` 是有效属性（非标 HTML，但被 site.js 读取），用于定位 `/img/octicons-16.svg` 代码块复制按钮图标
- 批量将 `data-ui-root-path="../../_"` → `data-ui-root-path="../../asset"`（各深度对应调整）
- 修改了 **1376 个文件**，1376 处替换

## 根相对路径转相对路径 + 删除 tppabs

- 将所有 `href="/docs/..."` `src="/docs/..."` 转为基于文件位置的相对路径（如 `../../asset/css/site.css`）
- 删除所有 `tppabs="..."` 属性（Teleport Pro 下载工具遗留，浏览器不解析）
- 转换了 **28,786** 处绝对路径，删除了 **47,308** 处 `tppabs` 属性
- 额外修正 `href="/docs"`（尾部无斜杠，指向网站根）**1,376** 处
- 最终确认：0 残留
