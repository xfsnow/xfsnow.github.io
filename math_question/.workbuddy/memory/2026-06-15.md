# 2026-06-15 工作日志

## Font Awesome → 内联 SVG 迁移

### 处理文件
- `index.htm`: 移除 FA CDN link，添加 11 个 SVG symbol 定义，替换 8 处 `<i class="fas fa-xxx">` 为 `<svg class="icon"><use href="#icon-xxx"/></svg>`
- `math_question.js`: 替换 12 处 FA 图标引用（含 innerHTML 动态拼接场景）
- `math_question.css`: 添加 `.icon` 样式类

### 替换的图标清单
plus, file-export, file-import, print, trash, save(floppy-disk), times(xmark), edit(pencil), eye, eye-slash(regular), lightbulb

### SVG 路径来源
全部从 Font Awesome GitHub 6.x 仓库 Raw 文件获取（WebFetch），确保 1:1 官方还原。

### 创建 Skill
- `font-awesome-to-svg` → `~/.workbuddy/skills/font-awesome-to-svg/SKILL.md`
- 包含完整步骤、FA5→FA6 更名对照、JS innerHTML 注意事项、viewBox 参考表
