# 2026-08-29

## 首页工具卡片：响应式图片处理 → GeoGebra手册中文版
- 将网站首页（`index.html`、`en/index.html`）以及模板（`assets/template/index.html`）中"响应式图片处理 / 在线图片压缩和格式转换工具"工具卡片替换为"GeoGebra手册中文版 / 根据官网GeoGebra手册英文版翻译的中文手册"。
- 图标使用内嵌 SVG（内容取自 `assets/img/geogebra.svg`，去掉 Tailwind 的 `w-16 h-16` 类，改用 `tool-icon` 类，viewBox 0 0 512 512，48x48）。
- 卡片链接假设为 `/geogebra/`（手册目录尚未创建，需用户确认/后续创建）。
- 同步更新数据文件：`zh/tools.json`、`en/tools.json`、`zh/article.js`、`en/article.js`（id 改为 `geogebra-manual`，icon 用 `fas fa-book`，en 版本使用英文翻译）。
- 注意：`BlogMaker.py` 会加载 `{lang}/tools.json` 渲染模板生成 index.html；首页 HTML 是压缩单行格式，模板是格式化格式。
